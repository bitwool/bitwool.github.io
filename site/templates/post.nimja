<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{post.title}}</title>
    <link rel="stylesheet" href="../style.css">
</head>
<body>
    <h1>{{post.title}}</h1>
    <p>{{post.date}} </p>

    {% for tag in post.tags %}
    {{tag}} 
    {% endfor %}

    {{post.content}}
</body>
</html>